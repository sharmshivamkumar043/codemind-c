#include <bits/stdc++.h> 
using namespace std;   
int main() 
{ 
    int a[10] = {1, 1, 2, 2, 3, 4, 5, 5, 6, 7}; 
    sort(a,a+7);   
    if(10%2==0)
    {
        cout<<a[10/2-1];
    }
    else
    {
        cout<<a[10/2];
    }
    
} 
